eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9090/eureka/
  instance:
    hostname: localhost
    
    
microservice:
  product-service:
    endpoints:
      endpoint:
        uri: http://PRODUCT-SERVICE/products
  
